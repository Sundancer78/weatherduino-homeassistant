{
  "config": {
    "step": {
      "user": {
        "title": "WeatherDuino",
        "description": "Enter the WeatherDuino IP/hostname.\n\nJSON path:\n- Default is /json (WeatherDuino 4Pro)\n- Leave empty to use / (root), e.g. for WeatherDisplay",
        "data": {
          "host": "IP/Hostname",
          "port": "Port",
          "path": "JSON path (leave empty for /)",
          "scan_interval": "Scan interval (seconds)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the WeatherDuino JSON endpoint.",
      "invalid_host": "Please enter a valid IP/hostname."
    },
    "abort": {
      "already_configured": "This WeatherDuino device is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "WeatherDuino Options",
        "description": "Adjust polling settings.\n\nJSON path:\n- Default is /json\n- Leave empty to use / (root)",
        "data": {
          "path": "JSON path (leave empty for /)",
          "scan_interval": "Scan interval (seconds)"
        }
      }
    }
  }
}
