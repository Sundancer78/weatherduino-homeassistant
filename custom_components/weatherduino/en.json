{
  "config": {
    "step": {
      "user": {
        "title": "WeatherDuino",
        "description": "Enter the WeatherDuino IP address. The integration will fetch JSON from /json.",
        "data": {
          "host": "IP address",
          "path": "JSON path"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to WeatherDuino JSON endpoint.",
      "invalid_host": "Please enter a valid IP address."
    },
    "abort": {
      "already_configured": "This WeatherDuino is already configured."
    }
  }
}
